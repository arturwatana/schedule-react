import styles from "./Footer.module.css";

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>Ambiente criado apenas para estudos</p>
    </footer>
  );
}

export default Footer;
